# Event Service Environment Variables

# Server Configuration
PORT=8003
NODE_ENV=development
API_GATEWAY_URL=http://localhost:8000

# Database Configuration (Required)
# For local development with Prisma, use a local SQLite file:
EVENT_DATABASE_URL="file:./event.db"

# For Cloudflare D1 local development with Wrangler:
# EVENT_DATABASE_URL="file:./.wrangler/state/v3/d1/miniflare-D1DatabaseObject/event-db/db.sqlite"

# Note: Cloudflare D1 doesn't use traditional connection strings
# It's accessed through Cloudflare Workers bindings
# For production, deploy as Cloudflare Worker or use D1 HTTP API

# RabbitMQ Configuration
RABBITMQ_URL=amqp://localhost:5672
RABBITMQ_EVENT_QUEUE=event-service-queue
RABBITMQ_RESPONSE_QUEUE=api-gateway-response-queue

# Cloudflare R2 Configuration (Optional - not used anymore, files uploaded via API Gateway)
# R2_BUCKET=your-r2-bucket-name
# R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
# R2_ACCESS_KEY_ID=your-r2-access-key-id
# R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
# R2_PUBLIC_URL=https://your-public-r2-domain.com
